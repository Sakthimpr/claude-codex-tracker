# Hygiene Note

## Workspace Cleanup Cadence
- Perform a root-level cleanup in `~/Documents/Codex` after major implementation sessions.
- Remove temporary verification artifacts (`*.png`, `*.html`, `*.js`, `*.map`) that are not part of active projects.
- Keep only active project folders and essential documentation.

## Current Canonical Tracker Project
- `~/Documents/Codex/claude_codex_tracker`

## Rule
- Avoid leaving loose debug or snapshot files at `~/Documents/Codex` root.
